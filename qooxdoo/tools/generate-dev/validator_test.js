// x follow after x
// Built-in, Protected, Name
hello world;
Array push();
this execute();

// Two functions calls
// hello() world();
// Any chance to detect this, without breaking if(foo)dosomething()?

// Test operators
i+ +3

// Double semicolon
hello();;

// Parenthesis
test(]);
test = {];
test = [);
