{
  "jobs" :
  {
    "common" :
    {
      "let" :
      {
         "QOOXDOO_PATH" : "../../framework" ,
         "QOOXDOO_URI"  : "../${QOOXDOO_PATH}" ,
         "CONTRIB_CACHE": ".",
         "APPLICATION"  : "custom"
      },

      "library" :
      [
        {
          "manifest"     : "${QOOXDOO_PATH}/Manifest.json",
          "uri"          : "${QOOXDOO_URI}"
        },

        /*
        {
          "manifest"     : "../../../../qooxdoo-contrib.trunk/SkeletonApplication/trunk/Manifest.json",
          "uri"          : "../../../../../qooxdoo-contrib.trunk/SkeletonApplication/trunk"
        },
        */

        {
          "manifest"     : "contrib:\/\/SkeletonApplication/trunk/Manifest.json",
          "uri"          : "../${CONTRIB_CACHE}/SkeletonApplication/trunk"
        },

        {
          "manifest"     : "Manifest.json",
          "uri"          : ".."
        }
      ],

      "settings" :
      {
        "qx.application" : "${APPLICATION}.Application",
        "qx.resourceUri" : "${QOOXDOO_URI}/source/resource"
      },

      "include" :
      [
        "${APPLICATION}.Application",
        "skeletonapplication.Application",
        "qx.theme.Classic"
      ],

      "cache" :
      {
        "path" : "../../cache"
      },

      "cache-downloads" :
      {
        "path" : "${CONTRIB_CACHE}"
      }
    },

    "source-script" :
    {
      "extend" : ["common"],

      "script" :
      {
        "file" : "./source/script/${APPLICATION}.js"
      }
    },

    "source" :
    {
      "run" :
      [
        "source-script"
      ]
    },

    "build-script" :
    {
      "extend" : ["common"],

      "compile" :
      {
        "target" : "build",
        "file" : "./build/script/${APPLICATION}.js",
        "uri"  : "script/${APPLICATION}.js"
      }
    },

    "build-files" :
    {
      "extend" : ["common"],
      "copy-files" :
      {
        "files" :
        [
          "index.html"
        ]
      }
    },

    "build" :
    {
      "run" :
      [
        "build-files",
        "build-script"
      ]
    }
  }
}
