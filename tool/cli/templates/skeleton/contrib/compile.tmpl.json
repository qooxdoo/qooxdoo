{
  "targets": [
    {
      "type": "source",
      "outputPath": "source-output"
    },
    {
      "type": "build",
      "outputPath": "build-output"
    }
  ],
  "defaultTarget": "source",
  "locales": ${locales},

  "applications": [
    {
      "class": "${namespace}.demo.Application",
      "theme": "${namespace}.theme.Theme",
      "name": "${namespace}"
    }
  ]
}