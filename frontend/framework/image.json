{
  // FRAMEWORK IMAGE CLIPPING AND COMBINING JOBS
  "jobs" :
  {
    "common" :
    {
      "let" :
      {
        "RESPATH" : "./source/resource",
        "MODERN" : "qx/decoration/Modern"
      },

      "cache" :
      {
        "path" : "../cache"
      }


    },


    "image-clipping" :
    {
      "extend" : ["common"],

      "slice-images" :
      {
        "images" :
        {
         // this is the input file
          "${RESPATH}/${MODERN}/form/button-pressed.png" :
          {
            "prefix" : "button-pressed",
            "border-width" : 5
          }
        }
      }
    },


    "image-combine" :
    {
      "extend" : ["common"],

      "combine-images" :
      {
        "images" :
        {
          // this is the output file
          "${RESPATH}/${MODERN}/form/button-tb-combined.png":
          {
            "input"  : [ "${RESPATH}/${MODERN}/form/button-pressed-*.png"],
            "layout" : "horizontal"
          },

          "${RESPATH}/${MODERN}/checkradio-combined.png":
          {
            "input"  : 
              [
                "${RESPATH}/${MODERN}/form/checkbox-*.png", 
                "${RESPATH}/${MODERN}/form/radiobutton-*.png"
              ],
             "layout" : "horizontal"
          }
        }
      }
    }
  }
}
