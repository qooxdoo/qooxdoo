{
  "include" : 
  {
    "appconf": "../tool/data/config/application.json"
  },

  "jobs" :
  {
    "common" :
    {
      "let" :
      {
        "QOOXDOO_PATH" : "..",
        "QOOXDOO_URI"  : "../${QOOXDOO_PATH}"
      },

      "library1" :
      [
        {
          "encoding" : "utf-8",
          "path"     : "${QOOXDOO_PATH}/framework/source",
          "uri"      : "${QOOXDOO_URI}/framework/source"
        }
      ],

      "library" :
      [
        {
          "manifest" : "Manifest.js",
          "uri"      : "${QOOXDOO_URI}/framework"
        }
      ],

      "settings" :
      {
        "qx.version" : "0.8"
      },

      "cache" :
      {
        "path" : "../cache"
      }
    },


    "api" :
    {
      "extend" : ["common", "appconf/jobs/api"],
      
      "let" :
      {
        "ROOT" : "../application/apiviewer",
        "BUILD_PATH" : "./api"
      },

      "library" :
      [
        {
          "manifest" : "${ROOT}/Manifest.js",
          "uri"      : "../${ROOT}"
        }
      ],

      "settings" :
      {
        "qx.resourceUri" : "./resource"
      },

      "api_include" : ["qx.*"]   
    }
  }
}
