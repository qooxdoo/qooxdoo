{
  "name"    : "${Name}",

  "include" :
  [
    {
      "path" : "${REL_QOOXDOO_PATH}/tool/data/config/application.json"
    }
  ],

  "let" :
  {
    "APPLICATION"  : "${Namespace}",
    "QOOXDOO_PATH" : "${REL_QOOXDOO_PATH}/framework",
    "QOOXDOO_URI"  : "../${REL_QOOXDOO_PATH}/framework",
    "QXTHEME"      : "${Namespace}.theme.Theme",
    "API_EXCLUDE"  : ["qx.legacy.*","qx.test.*"],
    "LOCALES"      : [ "en" ],
    "ROOT"         : "."
  },

  // You only need to edit the remainder of this file, if you want to customize
  // specific jobs, or add own job definitions.

  /*
  "jobs" :
  {
    "common" : 
    {
      "library" :
      [
        // Uncomment the following library entry to add a contrib or library to
        // your project; make sure to adapt the path to the Manifest.json and
        // the uri entry.
        {
          "manifest" : "contrib://SkeletonApplication/trunk/Manifest.json",
          "uri"      : "../cache-downloads/SkeletonApplication/trunk"
        }
      ]
    },

    // If you want to tweak a job setting, see the following sample where
    // the "format" feature of the "build-script" job is overridden.
    // To see a list of available jobs, invoke 'generate.py x'.
    "build-script" :
    {
      "compile-dist" : 
      {
        "format" : "off"
      }
    }
  }
  */
}
